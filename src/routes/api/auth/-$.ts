// Auth proxy route: forwards /api/auth/* to the Convex Better Auth HTTP action.
//
// TanStack Start API file routes (createAPIFileRoute) are not yet available in
// this package version. When the feature ships, replace this file with:
//
// import { createAPIFileRoute } from '@tanstack/react-start/api'
// import { handler } from '#/lib/auth-server'
//
// export const APIRoute = createAPIFileRoute('/api/auth/$')({
//   GET: ({ request }) => handler(request),
//   POST: ({ request }) => handler(request),
// })
//
// In the meantime, the auth client should be configured to point directly
// to the Convex site URL (CONVEX_SITE_URL) for auth requests.
export {}
